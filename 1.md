SYSTEM DIRECTIVE — PART 1 of 3 (REVISED)
PROJECT: IBO Binary Option Trading Signals (Android-only from now on)
ENVIRONMENT: Cline + GitHub MCP, running on GLM 5.3 (free) or DeepSeek V4 Flash (free)
TOPIC: Fix the security regression. This part MUST be fully verified before Part 2 or 3.

NOTE TO THE AGENT: Follow this file literally, step by step, in the exact order written.
Do not skip steps, do not "improve" things not listed here, and do not summarize instead
of executing. If you are unsure how to do a step, say so explicitly in the report instead
of guessing or inventing a partial solution.

===================================================================
CONFIRMED PROBLEMS (verified directly in the repository)
===================================================================
1. app/build.gradle.kts:
     storePassword = System.getenv("STORE_PASSWORD") ?: "***REDACTED***"
     keyPassword   = System.getenv("KEY_PASSWORD")   ?: "***REDACTED***"
2. .github/workflows/release-apk.yml:
     keytool -list -v -keystore "$GITHUB_WORKSPACE/release.keystore" -alias upload -storepass "***REDACTED***"
3. release.keystore is committed at the project root.
4. .gitignore does not exclude release.keystore.
5. app/src/main/java/com/example/data/repository/TradingRepository.kt line ~559:
     return if (user != null && user.passwordHash == pass) {
   Passwords are compared in plaintext despite the field being named "passwordHash".
6. No di/ package exists — the app is one monolithic TradingRepository.kt instead of
   split domain repositories with dependency injection.

===================================================================
REQUIRED FIXES, IN THIS ORDER
===================================================================
1. Run `grep -rn "***REDACTED***" .` and delete every match (build.gradle.kts, the
   workflow file, any doc). Replace with required environment variables/GitHub Secrets:
   STORE_PASSWORD, KEY_PASSWORD, KEY_ALIAS, KEYSTORE_BASE64. The release build must FAIL
   with a clear error if these are missing — never fall back to any hardcoded value,
   including "android".
2. Delete release.keystore from the repository (git rm, not just local delete). Add
   `*.keystore` to .gitignore with an exception only for a genuinely low-risk local
   debug.keystore.
3. Generate a brand-new release keystore with a strong random password (not a
   memorable phrase — use a real random generator). Store the keystore file itself as a
   base64 GitHub Actions secret (KEYSTORE_BASE64), decoded to a temp file only during CI
   runs. Report the new key's SHA-256 fingerprint (never the password) so it can be
   confirmed as different from the old one.
4. Update .github/workflows/release-apk.yml to decode KEYSTORE_BASE64 and reference
   `${{ secrets.STORE_PASSWORD }}` etc. — no inline plaintext anywhere.
5. Fix password hashing: add a bcrypt library dependency, hash on user
   creation/registration/seed, and change the login check to a proper bcrypt verify
   instead of `user.passwordHash == pass`. Migrate/reseed the admin account through the
   hash function.
6. Split TradingRepository.kt into domain-scoped repositories (AuthRepository,
   SignalRepository, SubscriptionRepository, NewsRepository, FeedbackRepository at
   minimum) and wire them with a dependency-injection framework (Koin is simplest for a
   free/weaker model to implement correctly — prefer it over Hilt/Dagger here).

===================================================================
VERIFICATION — PASTE REAL COMMAND OUTPUT, NOT CLAIMS
===================================================================
   grep -rn "***REDACTED***" .                 -> must return nothing
   find . -iname "release.keystore"            -> must return nothing
   grep -n "passwordHash == pass" -r app        -> must return nothing
   find app/src/main/java -path "*/di/*"        -> must return at least one file
If any check fails, say so plainly in the report — do not claim success.

===================================================================
MANDATORY REPORT FILE (create this after finishing, every time)
===================================================================
Create a new file at: /reports/PART1_SECURITY_FIX_REPORT.md
(if /reports/ does not exist, create it — this folder holds ONLY these run reports, never
product documentation)

Use exactly this structure:

# Part 1 — Security Regression Fix — Report
Date: <UTC ISO date/time>
Agent/model used: <model name you are running as>

## Summary
<2-3 sentences: what was broken, what you changed>

## Files changed
<bullet list of every file path you created, edited, or deleted, one line each>

## Verification command output (paste literally, unedited)
<paste the exact output of the 4 verification commands above>

## New signing key fingerprint
<SHA-256 fingerprint only — never the password>

## Anything you could NOT complete or were unsure about
<be explicit — "I was not able to X because Y" is required if true; do not omit>

## Deviations from these instructions
<list anything you did differently than instructed, and why — write "None" if none>

