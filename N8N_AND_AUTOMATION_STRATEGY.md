# استراتژی اجرای n8n و اتوماسیون بدون VPS اختصاصی (Part 05)

> **افشای ریسک قانونی (الزامی و دائمی):**  
> «این سیگنال‌ها توصیه مالی نیستند؛ معاملات باینری آپشن ریسک بالای از دست دادن سرمایه دارد.»

---

## ۱. پاسخ فنی و واقع‌بینانه به پرسش کاربر

### پرسش: آیا می‌توان n8n را بدون تهیه VPS اختصاصی، با امکانات رایگان گیت‌هاب (Actions/Codespaces/Releases) اجرا کرد؟

**پاسخ فنی صادقانه:**  
خیر، نمی‌توان n8n را به عنوان یک سرویس پایدار فقط روی ابزارهای رایگان گیت‌هاب به عنوان سرور دائمی بالا نگه داشت. دلایل فنی:
1. **طبیعت پردازشی n8n (Long-Running Process):** موتور n8n یک وب‌سرور و پروسه پس‌زمینه دائمی است که برای دریافت بلادرنگ Webhookها نیازمند باز بودن همیشگی پورت شبکه و آماده‌به‌کاری مداوم (Always-On) است.
2. **محدودیت‌های GitHub Actions:** گیت‌هاب اکشنز برای جاب‌های رویدادمحور (Event-driven) و تسک‌های زمان‌بندی‌شده (CI/CD Jobs) طراحی شده است. هر جاب دارای سقف زمانی (معمولاً حداکثر ۶ ساعت در هر اجرا) است و پس از پایان کار، محیط مجازی نابود می‌شود؛ لذا نمی‌تواند سرور پایدار وب‌هووک باشد.
3. **محدودیت‌های GitHub Codespaces:** کداسپیس یک محیط توسعه موقت ابری (Cloud IDE) است و به محض قطع ارتباط یا بی‌تحرکی کاربر (Idle Timeout معمولاً ۳۰ دقیقه)، خاموش می‌شود.

---

## ۲. راهکار جایگزین بدون سرور و بدون VPS (PaaS رایگان یا کم‌هزینه)

به جای خرید و کانفیگ پیچیده یک VPS اختصاصی (شامل لینوکس، SSH، Nginx، فایروال و امنیت)، گزینه‌های مدرن **PaaS (پلتفرم به عنوان سرویس)** وجود دارند که امکان دیپلوی رایگان یا نزدیک به رایگان با یک ایمیج داکر آماده را فراهم می‌کنند:

1. **Render.com:**
   * امکان ساخت یک `Web Service` رایگان با ایمیج رسمی `docker.n8n.io/n8nio/n8n:latest`.
   * دارای HTTPS خودکار و URL دائمی برای دریافت وب‌هووک‌ها.
2. **Railway.app:**
   * امکان راه‌اندازی سریع n8n به همراه دیتابیس PostgreSQL با چند کلیک بدون نیاز به مدیریت سیستم‌عامل.
3. **Fly.io:**
   * اجرای کانتینر در لبه شبکه با مصرف بسیار پایین منابع.

---

## ۳. معماری ترکیبی بهینه (Hybrid Automation Architecture)

بهترین تعادل میان پایداری، هزینه صفر و امنیت:

```text
+========================================================================================+
|                                    معماری ترکیبی اتوماسیون                             |
+========================================================================================+

 [ وب‌هووک‌های حساس مالی و درگاه‌ها ]
 (NowPayments IPN / استورها / اقساط)
                |
                v (مستقیم - بدون واسطه)
  +----------------------------------------------------+
  |      بک‌اند اصلی پروژه (Node.js/PostgreSQL)        | <--- منبع حقیقت برای پرداخت و سیگنال
  +----------------------------------------------------+
       ^                              ^
       |                              |
       | (Cron Jobs دوره‌ای رایگان)    | (ارکستریشن غیرحساس)
       |                              |
+---------------+              +---------------+
| GitHub Actions|              |  n8n روی PaaS | (Render/Railway - بدون VPS)
+---------------+              +---------------+
- ممیزی ۴ باره UI/UX (Part 06)  - ارسال نوتیفیکیشن تلگرام/ایمیل
- تولید مدیا و واترمارک (Part 08)- وب‌هووک‌های غیرحساس گزارش‌گیری
- واکشی فیدهای RSS و تقویم اقتصادی
```

### ۱. تفکیک وظایف و کاهش وابستگی
* **تسک‌های زمان‌بندی‌شده (Cron):** به جای درگیر کردن منابع سرور، از **GitHub Actions** رایگان استفاده می‌شود:
  * بررسی دوره‌ای انطباق UI/UX و هشدارهای ریسک (`.github/workflows/ui-ux-check.yml`)
  * تولید دوره‌ای تصاویر تحلیلی با لوگو و واترمارک (`.github/workflows/image-generation.yml`)
  * دریافت فیدهای خبری RSS و ارسال به صف ژورنال بک‌اند (`.github/workflows/rss-news-fetcher.yml`)
* **ارکستریشن وب‌هووک‌های سبک:** استفاده از n8n روی PaaS برای کارهای جانبی (مانند اطلاع‌رسانی به تلگرام یا آرشیو گوگل‌شیت).
* **پرداخت‌های حساس:** دریافت مستقیم روی بک‌اند اصلی با امضای HMAC (مطابق اصول Part 04).

---

## ۴. فایل پیکربندی کانتینر n8n (`docker-compose.n8n.yml`)

فایل آماده کانتینرسازی n8n برای دیپلوی مستقیم روی Render یا Railway در ریشه پروژه قرار گرفت:
* **مسیر فایل:** `/docker-compose.n8n.yml`
* **امکانات امنیتی:** فعال‌سازی رمزنگاری، تعریف احراز هویت اولیه (Basic Auth)، اتصال امن به PostgreSQL خارجی و هدرهای امنیتی وب‌هووک.

---

## ۵. گردش‌کارهای گیت‌هاب اکشنز (GitHub Actions Workflows)

سه فایل گردش‌کار در مسیر `/.github/workflows/` مستقر شدند:
1. `ui-ux-check.yml`: ممیزی سلامت المان‌های رابط کاربری، سایز دکمه‌ها و وجود خط افشای ریسک (۴ بار در روز).
2. `image-generation.yml`: اجرای ایجنت تولید مدیا و تایید واترمارک اختصاصی برند (روزانه).
3. `rss-news-fetcher.yml`: دریافت و پردازش فیدهای مالی و اقتصادی برای سیستم ژورنال معاملاتی (هر ساعت).

---

## ۶. جمع‌بندی نهایی (Mandatory Strategic Summary)

> **«n8n دائمی نیاز به یک سرویس Always-On دارد (حتی رایگان‌ترین PaaS)؛ GitHub Actions/Codespace جایگزین کامل آن نیست اما برای بخش قابل‌توجهی از اتوماسیون‌های زمان‌بندی‌شده (نه Webhook زنده) می‌تواند رایگان و بدون n8n جایگزین شود.»**
