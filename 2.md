SYSTEM DIRECTIVE — PART 2 of 3 (REVISED — REPLACES the previous "build a PWA" directive)
PROJECT: IBO Binary Option Trading Signals (Android-only from now on)
TOPIC: REMOVE the PWA/web project entirely. Focus 100% on the native Android app.
        Set up automatic version bump + signed release .apk build on every update.

IMPORTANT: This REPLACES the earlier "part2_pwa_frontend_rebuild.txt" instruction, which
you should now IGNORE if it was ever given to you. Do not build any web frontend. Do not
build any PWA. The product going forward is an Android APK only.

===================================================================
STEP 1 — REMOVE THE PWA/WEB PROJECT COMPLETELY
===================================================================
Delete the following, if present, in their entirety:
   frontend/               (the web project folder)
   public/                 (PWA/web static assets folder, if it only serves the web app)
   metadata.json            (AI Studio web-app metadata — not needed for an Android-only
                             project; confirm nothing else in the repo depends on it
                             before deleting, and say so in the report if something does)
   Any manifest.webmanifest, service worker, or index.html for the web app
   Any GitHub Actions workflow step that builds, tests, or deploys the web/PWA project
     (e.g. in ui-ux-check.yml, image-generation.yml, etc. — check each workflow file
     for frontend/PWA-related steps and remove only those steps, not unrelated ones)
Also remove any references to "PWA" or "frontend web app" from README.md and
ARCHITECTURE.md so the documentation matches reality (Android-only). Do not rewrite
unrelated sections of those docs.

===================================================================
STEP 2 — AUTOMATIC VERSION BUMP ON EVERY UPDATE
===================================================================
In app/build.gradle.kts, ensure versionCode and versionName are NOT hardcoded literals
that a human must remember to bump. Implement one of these (pick whichever is simplest to
implement correctly for your own capability — say which one you chose and why):
  (a) Derive versionCode from the count of git commits (or a stored counter file
      version.properties that auto-increments), and versionName from a semantic version
      stored in the same file, auto-incrementing the patch number on every build.
  (b) Derive both from the git tag that triggered the release workflow (e.g. tag v1.4.2
      -> versionName "1.4.2", versionCode computed deterministically from it).
Whichever you choose, it must require ZERO manual editing of build.gradle.kts by a human
before each release — the version must always increase automatically.

===================================================================
STEP 3 — RELEASE WORKFLOW BUILDS A SIGNED RELEASE APK EVERY TIME
===================================================================
Update (or create if missing) .github/workflows/release-apk.yml so that, on every push
that should trigger a release (e.g. push to main, or a version tag — pick one and state
which), it:
  1. Bumps the version per Step 2.
  2. Builds the signed release APK using the secrets set up in Part 1
     (STORE_PASSWORD, KEY_PASSWORD, KEY_ALIAS, KEYSTORE_BASE64) — never inline/hardcoded.
  3. Names the output artifact clearly with the version, e.g.
     IBO-TradingSignals-v<versionName>-release.apk
  4. Publishes it as a GitHub Release (or at minimum a downloadable workflow artifact) so
     a human can grab the latest APK without building locally.
  5. Fails the whole workflow (not silently continues) if signing fails for any reason.

===================================================================
VERIFICATION — PASTE REAL COMMAND OUTPUT, NOT CLAIMS
===================================================================
   find . -iname "frontend" -o -iname "metadata.json"      -> must return nothing
   grep -rn "PWA" README.md ARCHITECTURE.md                 -> must return nothing
   grep -n "versionCode\|versionName" app/build.gradle.kts  -> must show it is computed,
                                                                not a hardcoded literal
   cat .github/workflows/release-apk.yml                    -> confirm it references
                                                                secrets, not inline values

===================================================================
MANDATORY REPORT FILE
===================================================================
Create: /reports/PART2_ANDROID_ONLY_AND_RELEASE_PIPELINE_REPORT.md

Use exactly this structure:

# Part 2 — PWA Removal + Android-Only Release Pipeline — Report
Date: <UTC ISO date/time>
Agent/model used: <model name you are running as>

## Summary
<what was removed, what versioning approach you picked and why>

## Files/folders deleted
<bullet list>

## Files changed (versioning + workflow)
<bullet list with a one-line description of the change per file>

## Verification command output (paste literally, unedited)
<paste the exact output of the verification commands above>

## How to trigger a release build (explain in plain language for a non-developer)
<e.g. "push a tag like v1.5.0 and the APK will appear under GitHub Releases in ~X minutes">

## Anything you could NOT complete or were unsure about
<be explicit; do not omit>

## Deviations from these instructions
<list anything you did differently than instructed, and why — write "None" if none>

