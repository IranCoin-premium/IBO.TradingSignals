# جدول و مستند یکپارچه‌سازی درگاه‌های پرداخت (Part 04)

> **افشای ریسک قانونی (الزامی و دائمی):**  
> «این سیگنال‌ها توصیه مالی نیستند؛ معاملات باینری آپشن ریسک بالای از دست دادن سرمایه دارد.»

این سند تشریح‌کننده کامل نحوه اتصال به درگاه‌های پرداخت، مکانیزم‌های اعتبارسنجی سرور-به-سرور (Server-to-Server Verification)، بررسی امضاهای دیجیتال (HMAC-SHA512) و جدول نگاشت وضعیت سفارش‌ها بر اساس ضوابط سخت‌گیرانه امنیتی است.

---

## ۱. جدول نگاشت روش پرداخت به وضعیت‌های سفارش (Payment Method to Order State Mapping)

| روش پرداخت (`payment_method`) | وضعیت اولیه سفارش | رویداد / وب‌هووک میانی | وضعیت واسط | رویداد تسویه قطعی | وضعیت نهایی سفارش (`status`) | وضعیت اشتراک |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **مایکت (`myket`)** | `pending` | ارسال `purchaseToken` به سرور | `processing` | اعتبارسنجی موفق توکن از API سرور مایکت | `paid` | `active` |
| **کافه‌بازار (`bazaar`)** | `pending` | ارسال `purchaseToken` به سرور | `processing` | اعتبارسنجی موفق توکن از API سرور بازار | `paid` | `active` |
| **ایران‌اپس (`iranapps`)** | `pending` | ارسال `purchaseToken` به سرور | `processing` | استعلام تاییدیه رسمی از سرور ایران‌اپس | `paid` | `active` |
| **کارت‌به‌کارت (`card_to_card`)** | `pending` | آپلود تصویر فیش بانکی (`submit-receipt`) | `awaiting_manual_review` | تایید انسانی فیش توسط ادمین در پنل (`Approve`) | `paid` (یا در صورت رد: `failed`) | `active` |
| **شبا / پل / پایا / ساتنا (`shaba`)** | `pending` | ثبت کد ارجاع یا شناسه پیگیری انتقال | `awaiting_manual_review` | تطبیق واریز با صورت‌حساب بانکی توسط ادمین / PSP | `paid` | `active` |
| **NowPayments (تتر/کریپتو)** | `pending` | دریافت IPN با وضعیت `waiting`/`partially_paid` | `processing` | دریافت IPN معتبر با امضای HMAC و وضعیت `finished`/`confirmed` | `paid` | `active` |
| **دیجی‌پی (`digipay`) - ۴ قسط** | `pending` | ایجاد قرارداد اقساط و تسویه قسط ۱ | `processing` | دریافت وب‌هووک تسویه قسط اول | `paid` (با ثبت ۴ رکورد در `installments`) | `active` (معلق در صورت تاخیر اقساط) |
| **اسنپ‌پی (`snappay`) - ۴ قسط** | `pending` | ایجاد قرارداد BNPL و پرداخت قسط ۱ | `processing` | تایید کال‌بک سرور اسنپ‌پی برای قسط اول | `paid` (با ثبت ۴ رکورد در `installments`) | `active` (معلق در صورت تاخیر اقساط) |
| **گوگل‌پلی (`googleplay_pending`)** | — | کلیک کاربر روی گزینه گوگل‌پلی در فرانت‌اند | — | **غیرفعال** — باز شدن پاپ‌آپ چندزبانه «به‌زودی» (`googleplay_coming_soon`) | — | — |
| **درگاه‌های محلی سایر کشورها** | — | انتخاب درگاه محلی هدف | — | **غیرفعال** — نمایش برچسب «به‌زودی» تا اتصال رسمی | — | — |

---

## ۲. مشخصات اندپوینت‌ها و استانداردهای امنیتی

### ۱. پرداخت‌های درون‌برنامه‌ای استورها (In-App Purchases)
* **اندپوینت سرور:** `POST /api/v1/payments/:provider/verify` (با متغیرهای `bazaar`، `myket`، `iranapps`)
* **ورودی (Body):**
  ```json
  {
    "orderId": "UUID",
    "productId": "ibo_vip_monthly",
    "purchaseToken": "bazaar_purchase_token_string"
  }
  ```
* **سیاست امنیتی:** هرگز به ادعای مستقیم فرانت‌اند اعتماد نمی‌شود؛ بک‌اند مستقیماً توکن را از API رسمی هر استور استعلام می‌کند.
* **مستندات رسمی ارائه‌دهندگان:**
  * کافه‌بازار (Developer API): [مستندات اعتبارسنجی سرور بازار](https://developers.cafebazaar.ir/fa/docs/developer-api/)
  * مایکت (In-App Billing): [مستندات توسعه‌دهندگان مایکت](https://developer.myket.ir/docs/in-app-purchase)
  * ایران‌اپس: [مستندات درگاه ایران‌اپس](http://iranapps.ir/developers)

---

### ۲. واریز کارت‌به‌کارت و بارگذاری رسید
* **اندپوینت ثبت رسید:** `POST /api/v1/payments/card-transfer/submit-receipt`
* **ورودی:** `orderId`, `trackingCode`, `filePath`
* **اعتبارسنجی فایل:**
  * ذخیره فایل منحصراً در Object Storage امن (نه دیتابیس).
  * فرمت‌های مجاز: `jpg`، `png`، `pdf`.
  * حداکثر حجم مجاز: ۵ مگابایت.
* **وضعیت سفارش:** بلافاصله به `awaiting_manual_review` تغییر می‌یابد.
* **اندپوینت بررسی ادمین:** `POST /api/v1/payments/admin/receipts/:id/review`
  * نقش مجاز: `ADMIN` یا `SUPER_ADMIN`.
  * در صورت `approved`: سفارش `paid` شده و اشتراک کاربر آنی فعال می‌گردد.
  * در صورت `rejected`: سفارش `failed` شده و دلیل رد به کاربر اعلام می‌شود.

---

### ۳. درگاه رمزارز و دارایی دیجیتال (NowPayments)
* **ایجاد فاکتور پرداخت:** `POST /api/v1/payments/digital-asset/create-invoice`
* **وب‌هووک دریافت نوتیفیکیشن پرداخت (IPN):** `POST /api/v1/webhooks/nowpayments`
* **اعتبارسنجی امضا (HMAC-SHA512 Verification):**
  * سرور هدر `x-nowpayments-sig` را دریافت می‌کند.
  * با مرتب‌سازی کلیدهای جیسون بدنه (Sorted JSON) و کلید محرمانه `NOWPAYMENTS_IPN_SECRET`، هش HMAC محاسبه و تطبیق داده می‌شود.
  * **در صورت عدم تطابق امضا، درخواست بلافاصله با خطای ۴۰۱ رد شده و وضعیت سفارش تغییر نمی‌کند.**
  * تنها با دریافت وضعیت‌های `finished` یا `confirmed`، سفارش به `paid` تغییر می‌یابد.
* **مستندات رسمی:** [مستندات رسمی NowPayments API و IPN](https://documenter.getpostman.com/view/7907941/S1a32n38)

---

### ۴. پرداخت‌های اقساطی ۴ قسطه (دیجی‌پی و اسنپ‌پی)
* **اندپوینت ایجاد قرارداد اقساط:** `POST /api/v1/payments/installments/create-plan`
* **تولید خودکار اقساط:** ایجاد ۴ رکورد در جدول `installments` با موعد‌های ماهانه منظم و مبالغ معین.
* **وب‌هووک پرداخت اقساط:** `POST /api/v1/payments/webhooks/installments/:provider`
* **فعال‌سازی اشتراک:** با دریافت تاییدیه تسویه قسط اول (`installment_number: 1`)، وضعیت سفارش به `paid` تغییر کرده و اشتراک فعال می‌شود.
* **قوانین تعلیق و مهلت (Grace Period):** در صورت تاخیر بیش از ۷ روز در تسویه اقساط بعدی، دسترسی سیگنال تا زمان واریز معلق می‌شود.
* **مستندات رسمی ارائه‌دهندگان:**
  * دیجی‌پی: [مستندات تجاری و درگاه دیجی‌پی](https://www.mydigipay.com/developers/)
  * اسنپ‌پی: [مستندات وب‌سرویس پذیرندگان اسنپ‌پی](https://snapppay.ir/merchant-api/)

---

### ۵. درگاه گوگل‌پلی (سیاست تعلیق و عدم اتصال مستقیم)
* **اندپوینت استعلام وضعیت:** `GET /api/v1/payments/google-play/status`
* **پاسخ ساختاریافته:**
  ```json
  {
    "status": "success",
    "available": false,
    "translationKey": "googleplay_coming_soon",
    "message": "درگاه پرداخت گوگل‌پلی به زودی راه‌اندازی می‌شود."
  }
  ```
* **سیاست فرانت‌اند:** در کلاینت، دکمه با برچسب غیرفعال نمایش داده شده و کلیک روی آن پاپ‌آپ چندزبانه بر مبنای جدول `translations` را فراخوانی می‌کند. هیچ تراکنش واقعی تا اخذ مجوزهای مربوطه برقرار نمی‌شود.

---

## ۳. ممیزی امنیتی و ثبت ردپا (Audit Trail)
برای تمامی تصمیمات اعم از دریافت وب‌هووک، تایید رسید بانکی توسط ادمین و فعال‌سازی اشتراک‌ها، رکوردهای تغییرناپذیر در جداول `webhooks_log` و `audit_logs` درج می‌گردند تا فرآیند تسویه مالی کاملاً قابل حسابرسی و پیگیری باشد.
